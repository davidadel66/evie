# Memory Stage 5 held-out curation v1

This corpus was independently authored by agent `/root/heldout_curator` for
issues #154/#168 using the frozen integrated-development-v3 protocol and input
schema. It contains exactly 24 new cases, two per behavioral family. The curator
began without development-reader history and used the permitted rubric only for
its abstract obligations. This is agent curation, not independent human review.

The workload was written once at **2026-09-11T05:45:55.955804+00:00**.
Its authoring seal was recorded at **2026-09-11T05:50:49.798621+00:00**:

```text
SHA256 f487649e46249ab1f461f2c4b8551c12a5b6ff319461856d2d733ef9420a3cdd
Bytes  76407
```

This authoring seal is distinct from the canonical preparation and release seal
that root must perform with the exact development-verified executable. No
retrieval, embedding, reader output, or performance result was generated or used
to select or revise these cases. There is no release-readiness claim.

| Family | Independent obligations |
| --- | --- |
| fresh_accepted_preference | Sewing-pattern format; audiobook narration |
| uncompiled_original_statement | Rye-dough observation; projector rattle observation |
| cross_topic_reference | Clear choir-feedback recipient; ambiguous onboarding recipient |
| compacted_continuity | Clear equipment-handover recipient; ambiguous volunteer pickup |
| exact_identity | Accepted Quill Alias; explicit subject Entity-ID placeholder |
| graph_and_dense_paraphrase | Two accepted observatory relationships; screen-free errand paraphrase |
| bounded_neighbor_investigation | Owner backing selection from neighbors; provisional cabinet option and assistant-suggested test |
| historical_and_retired | Retired archive pickup with known half-open validity; current print returns with retired-source suppression |
| conflicting_claims_and_newer_statement | Accepted volunteer-shift disagreement plus explicit update; catering-counter disagreement plus tentative update |
| speaker_quote_and_tentative_attribution | Owner reporting another speaker; assistant suggestion versus owner preference |
| unanswerable_and_unavailable | Unknown serial suffix; unavailable sculpture memory |
| scope_and_no_memory_needed | General active-voice rewrite; permitted Global invoice preference with forbidden other-project and raw Global sources |

Every case declares one minimal support set, acceptable context, forbidden
current records/text, answer components, response kind, attribution and reader
rules before any release output. There are 51 source records, 28 required support
records across 19 answerable cases, and 36 personal answer components. These are
the actual held-out denominators, not copied development counts. The oracle
never needs more than two accepted and two conversation items in one case.

The corpus has exactly 2 `material_clarification`, 2 `clear_reference`, and 15
`critical_semantic` cases. The critical cohort retains fresh preference,
uncompiled original, clear cross-topic reference, both neighbor/authority
obligations, historical validity, current retirement suppression, both conflict
updates, both speaker obligations, evidence gap, unavailable memory, general
rewrite and scope containment. Original-source inspection after correction and
restart remains root's separate deterministic operating obligation.

Static validation passed with
`python3 /private/tmp/evie-memory-stage5-validate-heldout.py`. It checks closed
field shapes, enums, unique identifiers, support references, scope consistency,
original source text, accepted alias origins, graph Entity reuse, exact-ID
binding, assistant pairing, known historical bounds, source lengths, and the
family/role cohorts. A first validator attempt imposed an unnecessary exact
substring condition on the compound pattern-format literal; only that validator
was adjusted to recognize the exact documented semantic equivalence. The
workload did not change. `git diff --check` passed; direct whitespace checks also
cover the new untracked files.

One context qualification is retained. **After all case bytes were authored**,
a `collaboration.list_agents` inventory unexpectedly included a completed
implementation-review finding about conflict refresh across authorized scopes.
The curator had requested only slot availability. Root was notified, and the
corpus was preserved unchanged. No pre-exposure hash was measured or claimed.
No development reader answers, retrieval traces, prior held-out contents, or
release outputs were exposed. Root confirmed that this inventory does not itself
breach the frozen excluded-output rule and retained the qualification. The
machine-readable record preserves the exposure and actual event order. An early
filename inventory also showed reference-reader fixture names; their contents
were never opened or used.

[curation-record.json](curation-record.json) records the actual curator identity,
allowed file hashes, restrictions, timestamp/hash, static attempts, incident,
and skipped checks. Canonical preparation and sealing are pending root work.
The full repository verifier is deferred to root's release verification; these
curator changes are documentation/fixture data only. No production files, Git
index state, commits, remote branches, or external applications were modified.
