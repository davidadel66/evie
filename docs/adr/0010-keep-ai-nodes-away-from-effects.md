# Keep AI nodes away from external effects

AI Nodes return only typed proposed data and cannot directly perform external
effects. Deterministic nodes validate model output, enforce formulas, identity,
limits, and Standing Authority, and either continue through an explicit
capability node or durably interrupt the run. This preserves bounded model
judgment without allowing probabilistic reasoning to bypass business rules or
effect controls.
