interface:
  display_name: "Implement Story"
  short_description: "Implement one story in an isolated worktree"
  default_prompt: "Use $implement-story to implement exactly one ready story in an isolated worktree, verify it, and return a clean committed candidate for independent review."
