interface:
  display_name: "Plan Request"
  short_description: "Plan work and ready one selected story"
  default_prompt: "Use $plan-request to plan this request or prepare one explicitly selected story as a ready GitHub execution-contract issue."
