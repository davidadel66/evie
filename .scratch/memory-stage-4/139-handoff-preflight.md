# Ticket139 bounded historical backfill

Read published-bodies/08-bounded-historical-backfill.md, binding work/evidence decisions+fixtures and implement skill. Build on frozen138/new-evidence scheduling and137 worker. Keep historical request separate from live activation. Source coordinates include100-range/10000-event bounds, inclusive sequence boundaries plus exact event IDs; capture upperbounds atomically. Legacy events have no fabricated commit position. Authorize each source lineage and destination independently; no scope union that widens authority. One request may contain multiple sessions/scopes only via exact authorized range records.

Union overlapping same-generation/destination selections, reuse owned intervals/jobs/attempt budget, and keep immutable original request receipts. No skip action. Capture cutoff does not force live closure; retain revisitable pending selection. Every background transaction inspects at most128 source events or mutates64 candidate/ledger rows. Count queries/coordinate indexes are bounded separately; don't silently lower evidence policy to meet a query budget.

Use historical lane/oldest selection order in atomic selectCompilerUnitInTransaction and shared persistent8new:1historical scheduler. Reconciliation/cancellation must respect previously committed ownership; cancelling one request must not silently cancel work still selected by another overlapping request. Define/use union reference semantics from work contract, report any true unresolved behavior conflict to root. Pauses/cancel/resume retain exact attempt and generation fences. No implicit new generation/backfill on activation.

Coverage requires exact successful and excluded intervals, distinct completed_empty, gaps, each contiguous segment frontier, outside-selection history; never fabricate one cross-session frontier. Candidate acceptance/rejection is unrelated to completion. Source omission/control rows must be accounted without becoming support. Bounded paginated CLI selection/inspection/cancel on shared Kernel, realSQLite multisession/scopes, queuefull, crash/reopen, priorfailedlatercompletion, overlaps, race/cancel, fairness. Root integrates minimal main/db/host hooks if needed. Preserve user changes. No commit/stage/branch/push; freeze exact originals and owned files/hashes for root two-axis review, full verification and one ticket commit.
