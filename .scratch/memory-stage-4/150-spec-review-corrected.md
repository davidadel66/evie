# Ticket 150 corrected engineering Spec review

Scope: `git diff d12470f707ba5cc4ae3f4f45df4b3f2fc4361871 413601a7c6088b59fb5dcd7ced53f46193590ffe`, exact snapshot recorded in `150-corrected-checkpoint.json`. Initial static review followed by one newly authorized public Store reproduction; no broad tests or measurements were rerun, and no production files were edited.

**One remaining P2 finding: interleaved historical ownership can still manufacture an empty failed obligation.** At `internal/eviedb/compiler_reconciliation.go:92–100`, reuse is skipped when a newly discovered root member exceeds the previous sealed cutoff. Consider public event order A user1/final2 (already sealed), B user3/final4, then late A child5/final6. Explicitly select and materialize A's historical suffix5..6 before live ancestry discovery reaches5. Reconsidering A yields cutoff5 (its next selected root B3 has no predecessor at/after last5); the existing A owners are1..2 and5..6. `nextCompilerInterval` returns the coordinate gap3..4, whose events belong solely to B. Source capture therefore records another zero-member `failed:empty_selection` job. The new reuse guard handles later foreign coordinates when `last` is already owned by the immediately referenced selection, but not this already-owned historical suffix separated by foreign members.

The binding work contract requires “its exact root-member event IDs,” says “Root membership follows ancestry,” and requires overlapping historical requests to “reuse existing owned intervals and jobs.” The activation ticket requires reconciliation of “selected uncovered durable evidence.” Coordinate gaps containing no members of this root are not new evidence obligations. Skip such empty gaps without changing existing ownership or coverage, and add this interleaved public activation/history regression. Confirmed on the exact snapshot with `go test -overlay /Users/davidboktor/code/evie/.scratch/memory-stage-4/150-review-historical-gap-overlay.json ./scripts/memory-stage4-pilot -run '^TestPilotReviewedHistoricalSuffixDoesNotManufactureForeignRootGap$' -count=1 -v`: FAIL, 0.263s, extra failed interval3..4 with zero attempts and zero selected events. Raw evidence is `150-review-historical-gap-red.log`; the standalone public regression is `150-review-historical-gap-regression_test.go`. Only scratch overlay files were added. Root and owner received the reproduction.

No further actionable finding in the outcome-validation delta: exact totals, allowed outcomes, attempt counts, selected/completed coverage and dispatch counts now invalidate unexpected work after retaining diagnostics. The original study remains disqualified rather than being relabeled. Actual corrected measurements/report additions and required model/human observations remain outside this verdict.


## Follow-up on first repair

The live-gap fix in tree `52842eb744372dbf8a24df99859c6c84b5efda1a` passes the focused prior-boundary, live-gap and direct-error regressions independently (0.593s), but the same defect remains when broad historical reconciliation runs before live creates its marker. The public history-first variant independently failed in 0.281s with another failed3..4 zero-member job. Evidence: `150-review-history-first-red.log`; standalone regression: `150-review-history-first-regression_test.go`. The owner is correcting the automatic historical lane while preserving direct QueueCandidateUnit errors. Final narrow clearance remains pending.
