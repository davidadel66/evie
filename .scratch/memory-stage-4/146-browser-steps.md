# Ticket 146 deterministic browser demonstration

Use root's `.scratch/memory-stage-4/146-browser-fixture` against the final isolated source and a fresh temporary database. These are agent-driven demonstration steps, not David pilot observations.

1. Open Memory → Review candidates, choose Global memory. Source sessions are closed by the fixture.
2. Inspect the simple café candidate. Choose Edit interpretation, change Edited object value, enter Edit reason, and Record edited interpretation. Inspect before/after lineage and original evidence. Close the edit draft; Preview acceptance must show v5 member/effect/record disclosure. Confirm only after reading the exact preview.
3. Inspect the first Maya identity candidate. Inspect identity alternatives. Read the same-name alternatives/Aliases/context, explicitly choose the distinct new Entity and displayed new global Predicate, then Record these identity choices. Add inspected candidate to batch. Repeat the explicit choices for the second Maya candidate and add it to batch.
4. Give both selected candidates the same Atomic group name. Add explicit dependencies from candidate 2 subject to candidate 1 subject, and from candidate 2 predicate to candidate 1 predicate. No automatic identity matching occurs. Optionally add an unrelated inspected candidate as a separate reject group.
5. Prepare complete batch preview. Read every group's action, member meaning, original source authority, complete identity/Predicate/Alias/Claim/Source Link effects, dependency closure and semantic record transitions. Confirm all displayed group actions. Verify durable per-group receipts and operation provenance. Successful groups are not retried when another group fails; failed groups require newly inspected candidate refs and a fresh explicit preview.
6. Inspect the correction candidate. Inspect correction alternatives, choose the exact prior claim and offered error/changed mode, record that interpretation, then preview the exact old/next validity, polarity and lifecycle transitions before deciding. No date is inferred from an unknown effective time.
7. Inspect the clock candidate and its acceptance preview. Verify original owner versus tool_observation authority, local-clock-display-v1 contract/root/execution/call/ancestry metadata and exact unzoned display; no UTC effective instant is invented.
8. Refresh scopes/inbox and inspect the isolated session scope. Confirm exact scope navigation and that decided candidates are absent after reload. Recorded results/operation inspection are authorized independently of the source session's closed state.

The HTTP tests deterministically cover savepoint-local failures, postcommit response loss, exact retry after DB reopen, invalid/stale/source-policy cases and transport-byte boundaries. Browser sessionStorage retains only one exact delivery request (scope/key/preview ID/digest/actions/reason), capped at 32 KiB UTF-8 bytes. It never stores proposal/evidence/preview bodies. A reload offers explicit Recover earlier decision; it never automatically replays failed groups or invents a refreshed approval.
