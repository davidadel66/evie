# #151 Standards review — corrected static snapshot

Reviewed the exact 13 files in `151-review-fix-static-manifest.json` / `151-review-fix-static-files/` against repository `AGENTS.md` and the complete code-review smell baseline, following the initial `ebb84af9…` → `eebe81f9…` review. All manifest SHA-256 values and sizes match. Static inspection only: builds/tests remain deferred during #150 measurements. The owner has not yet frozen the final generated report or completed verification.

Both initial P2 findings are resolved in the inspected code: the index and receipts now use the same `os.Root` descriptor, with a directory-replacement boundary regression; immutable report publication now syncs the parent directory and preserves a published file on sync failure.

**P2 — Check availability for every required repetition.** `internal/memoryeval/stage4_release_observations.go`, `inspectStage4Measurements`, checks only whether the pooled per-workload `ObservationCount` is zero. One zero-denominator observation is therefore dropped while another repetition supplies enough observations for coverage to pass. For example, changing one report-only `completed_events_per_second` candidate denominator to zero leaves readiness passing because the other repetition remains populated and no numerical gate covers that metric. This contradicts the new command README contract, “A measured zero denominator remains unavailable and blocks readiness,” and `AGENTS.md`'s requirement to enforce boundaries with deterministic checks. Validate availability for each required baseline/candidate workload/repetition; add a single-unavailable-repetition regression, since the current zero-usefulness test makes all repetitions unavailable.

No additional actionable smell finding. The observation catalog and shared measurement binding remain focused on the required reporting contract. Final experimental evaluation remains unrun; #151 must remain open. Initial findings are preserved in `151-standards-review-initial.md`.
