# Spec review — ticket 151 engineering slice

Pinned diff: `git diff ebb84af94f37b5bb5278d54208dc1d9f2dba73c7 eebe81f9b39587405f9c2662ec0f94d68f9ad38d` (12 new files). Reviewed the ticket, active Stage 4 spec, binding decisions, owner handoff, implementation and tests. No source edits or full-suite rerun.

1. **[P2] Reject contradictory history-to-family assignments.** `internal/memoryeval/stage4_release.go:365–374` validates unique case IDs but allows the same `HistoryID` in different narrative families. Those entries then count as independent clusters. The spec requires “Keep narrative variants together” and “uncertainty where meaningful.” A public-seam overlay probe changed the existing passing example to one unique history under its two original family labels, rebound the frozen plan, and still obtained `ready=true` and `independent_narrative_families=2`. Require a single family per history (while permitting multiple windows in that family), and add a rejection regression. Probe: `go test -overlay .scratch/memory-stage-4/151-spec-probe/overlay.json ./internal/memoryeval -run '^TestSpecProbeSameHistoryCanManufactureClusters$' -v -count=1` against the isolated snapshot; confirmed in 0.175s. No model calls.

2. **[P2] Preserve required performance and review measurements before readiness.** `internal/memoryeval/stage4_release_gates.go:10–26` makes nine infrastructure metrics exhaustive and rejects every other name. The spec explicitly requires “queue and inference latency, throughput,” “model/host memory,” and “accept/edit/reject outcomes, and inbox age.” Several of these have neither a typed observation nor a report field; the existing complete example reports ready without any of them. Hashing an opaque artifact does not make the missing measurements visible in the assessment. Extend the frozen observation/report contract to retain these required measurements (with pilot-defined gates where appropriate), or keep readiness pending until an explicit complete protocol receipt supplies and reports them. Do not invent thresholds.

The pending committed report and open-ticket status are appropriate. No model execution, corpus loading, automatic activation, or Stage 5 claim was introduced. Hash authenticity and human semantic adjudication remain correctly documented external trust boundaries. These findings concern consistency and required coverage, not those trust boundaries.
