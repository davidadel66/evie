# Ticket 150 preliminary Spec review

Scope: `git diff ebb84af94f37b5bb5278d54208dc1d9f2dba73c7 b87d22cdd88034a029b1eba5b0ddf4af8313582f`; isolated snapshot `evie-150-tooling-checkpoint-xsbgpeoz`. Nine new tooling/preparation files; no ticket commit yet. This is a pre-measurement review, not final pilot acceptance.

1. **P2 — Source throughput uses incompatible event units.** `scripts/memory-stage4-pilot/run.go:286` assigns `foreground_source_events = 2 * Turns`, but real Agent.Send persists user, context snapshot, and assistant events. `compiler_source.go:219` includes the snapshot in `NewEventIDs`, so completed coverage counts three events while offered-source throughput counts two. The ticket requires “State both source-arrival versus compilation capacity” with “counts, denominators.” Measure actual persisted event deltas and use the same event population, or explicitly separate evidence-event and coverage-event rates. Fix before running the matrix.

2. **P2 — Added production files bypass conformance binding.** `scripts/memory-stage4-pilot/measure.py:104–112` only compares paths already listed in the conformance receipt. An additional `internal/eviedb/*.go` file can alter the linked program while validation still succeeds. This contradicts “preserve exact conformance.” Compare both production source path sets and hashes, permitting only explicitly scoped pilot additions. An independent direct validator probe confirmed this acceptance; no source files were altered for the probe.

3. **P2 — Missing/malformed trial receipts abort failure reporting.** `scripts/memory-stage4-pilot/measure.py:218–221` parses JSON and unconditionally hashes the report before the failed-trial branch. A process that exits before creating a report, or leaves truncated JSON, raises an uncaught exception and never writes the versioned matrix result. The ticket requires “record actual workloads and limits” and “Publish a reproducible versioned pilot report.” Preserve available raw artifacts, represent missing/invalid reports explicitly as failed trials, and write the aggregate failure outcome.

Static review plus the narrow source-validator probe only; no code edits or full tests. Actual model quality, David sessions, adopted numerical gates, and final results remain pending as explicitly disclosed. No scope-creep finding. Final results require a subsequent independent delta review.
