# Bound Workspaces to reviewed Agent Presets

Each Workspace Revision declares the Agent Presets allowed in that Workspace
and identifies one default. This bounds which capabilities can appear in its
sessions, while Workspace Access and Standing Authority independently bound
which data and consequential actions those capabilities may reach.
