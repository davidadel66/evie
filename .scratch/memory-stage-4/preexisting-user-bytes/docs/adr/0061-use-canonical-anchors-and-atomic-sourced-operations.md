# Use canonical anchors and atomic sourced operations

Stage 3 creates stable global owner and Evie Entities plus one scope-local
Context Entity for each Workspace, project, or session when first needed, with
accepted operations recording every generated identity for exact replay. One
approved compound operation may reuse or create all required Predicate, Entity,
Alias, Claim, and Source Link effects atomically rather than requiring approval
per row. Stage 3 mutations are session-bound and cite the exact owner-request
event; eventless inspection, verification, and maintenance cannot create
unsourced Semantic Memory.
