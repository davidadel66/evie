# Use one Context Scope per session

The first implementation permits one Workspace, one filesystem project, or no
Context Scope per session, in addition to global and session scopes. A session
cannot combine Workspace and project data. Explicit reviewed links between a
project and Workspace remain a later extension if a concrete need justifies the
additional cross-scope boundary.
