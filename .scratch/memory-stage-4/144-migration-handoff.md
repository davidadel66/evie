# Bounded subtask of144: compound correction schema migration

Implementation owner implement_144_edits_batches reserved these files for a separate migration agent: internal/eviedb/semantic.go (schema constant+ensureSemanticSchema hook), new internal/eviedb/candidate_review_correction_schema.go and matching migration-specific tests. Root orchestrates one144 commit; migration agent must not commit/stage/branch or modify owner compound implementation. Coordinate exact file ownership with144 owner before writes, save originals/hashes and freeze files separately for owner to incorporate into144 manifest. Preserve pre-existing user semantic_graph.go and all unrelated work.

Current semantic_claim_corrections operation_id TEXT PRIMARY KEY permits only one correction per accepted operation. V5 compound apply invokes existing applyReviewCorrectionEffect per member with same operation_id/scope_revision/transaction_time and distinct old_claim_id/replacement_claim_id. Change PK to(operation_id,old_claim_id), retain UNIQUE old_claim_id and UNIQUE replacement_claim_id, FKs, all mode/time/positive-revision constraints, scope index and append-only UPDATE/DELETE triggers. Preserve every existing row/column byte and old v1-v4 canonical operation/preview encodings. No new runtime dependency.

ensureSemanticSchema in semantic.go runs for real+shadow databases; integrate narrow idempotent schema detection/migration there. Audit readers/foreign references and shadow/rebuild. Existing production correction readers use old_claim_id; quarantine scan by operation_id can see duplicateoperation IDs but must remain correct. Migration must be transactional, tolerate concurrent OpenDBAt and reopen, retain old rows on failed/cancelled upgrade, fail closed on malformed/incompatible existing schema rather than silently discarding data. No rewrites of historical evidence/accepted operations.

Test a populated genuine Stage3 old-schema database, old correction rows/lifecycle+source bytes and canonical replay before/after, migration rollback/commit interruption where available, concurrent/repeated startup, multiple corrections under one operation and duplicate old/replacement refusal, append-only triggers/index/FKs and shadow schema compatibility. Coordinate exact v5 replay tests with144 owner; migration can test relational guarantees without fabricating new canonical acceptance as proof of replay. Run focused normal/race; root runs full exact144 checks/reviews. Build UI first for isolated CLI embeds if needed. Final handoff includes files[{path,before_sha256,after_sha256,bytes}], frozen directory and verification results. No standalone ticket or separate commit.
