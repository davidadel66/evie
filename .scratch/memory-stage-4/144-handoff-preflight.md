# Ticket 144: owner edits and dependent batches

Read the published ticket13, binding review contract R3/R4 and scenarios R05/R08/R10-R12/R15. Build on committed141 identities,142 temporal v3 and143 clock observation v4. Preserve every earlier candidate/preview/operation version and its canonical goldens. Kernel owns acceptance semantics, scope/provenance, validation and replay; CLI is a thin exact adapter.

Edits append immutable interpretations under the same unresolved candidate with parent revision, exact before/after meaning and reason (<=4KiB, secret-scanned), CAS both interpretation/review revisions and owner audit. Original extraction/support/window/generation are immutable. Changed evidence may use sufficient ranges only from the same frozen authorized window; no unrelated source, authority upgrade, destination change, or edit-text-as-evidence. Terminal candidates return already_resolved. Structural binding does not prove entailment.

Batches have one exact destination, <=20 groups, <=64 refs, <=256 enumerated semantic records, <=256KiB complete canonical preview bytes. Inclusive ceilings are hard. Each group is complete dependency closure; independent groups cannot share proposed identities, write same semantic object, or depend on another success. Reject invalid dependency labels/cycles/incomplete closure; do not silently split or truncate. Prepare exact generated IDs and before/after effects, actions, dependencies, original authority and complete source/context disclosure. One immutable preview/digest binds ordered group actions and read/write scope vector; source-policy or external revision drift stales whole preview before any group.

One outer SQLite write transaction with per-independent-group savepoints: own earlier successful groups may advance only enumerated revisions; no rebase of external changes. Each success atomically records canonical compound operation, sources/context, graph/revisions, candidate resolutions, audit and group result. Safe group-local errors roll back that entire group while independent groups continue; outer commit/cancellation failure rolls back everything. Final ordered delivery receipt is immutable, including failed groups. Same-key identical retry returns it and never retries failed groups; changed request conflicts. A fresh failed-group attempt needs new explicit preview/approval.

Tests: real SQLite edits + competing stores, mixed identity/temporal/clock candidates through CLI, complete before/after source-authority inspection, same-vector successful groups, injected group-local source/semantic failures, commit/cancel rollback, postcommit lost-response retry, changed source/policy/global revision, exact/max+1 bounds and byte/hash goldens, canonical model/tool-free replay. Coordinate shared files; save originals, freeze manifest. Root handles reviews, full checks and one commit. No new dependency, automatic configuration, model calls or implicit Promotion.
