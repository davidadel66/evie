# Ticket #141 identity/Predicate review handoff

Root seam notes. Implement assigned ticket published-bodies/10-identity-and-predicate-review.md with implement skill, after #140 fixed checkpoint. Read binding owner-review/evidence contracts and relevant Stage3 canonical graph operations. Do not invent source authority or learned matching. #137/#138 workers own compiler scheduling/schema/work; coordinate any narrow additions there. Root commits one exact ticket and preserves user work.

Current model boundary ExtractorCandidate contains only a ClaimProposition with existing identities, ValidTime/qualification and evidence/context locators. #140 preview v1 accepts exactly one existing-identity CandidateRef and creates/reuses a Claim + Source Links. Source interpretation context is recorded in canonical v6 operations and verified historically. Extend this coherent model to explicit proposed identity/lexical alternatives and Predicate definitions; model names alone never mint accepted identity or global Predicate meaning. Owner choices are preview-bound and require source support/current scope. Existing Stage3 canonical entity/alias/Predicate/claim effects are the implementation vocabulary.

Generation compatibility is a real boundary: CompilerPolicyVersion is currently owner-assertions-v1 and CompilerGenerationIdentity accepts that version for each pinned policy. New admitted proposal/effect types change the contract and need explicit policy/version handling. Do not silently widen old generations. Retained requests, candidates, previews and accepted v6 operations must keep their canonical bytes/hash/replay behavior. Add backward-compatibility vectors when extending structures, especially the strict shape-preserving DecodeCompilerJSON and JSON field omission behavior. Versioned effect/preview encodings currently have a golden UTF8 fixture; preserve its old exact encoding instead of rewriting it to accept the new schema accidentally.

Relationship/new Entity/new Predicate dependencies must be fully explicit and atomic for this candidate group. Extraction job is not automatically one review group. Same-name alternative sets stay unresolved until owner choice; no automatic merge from name or confidence. Broader scope references require established Promotion; a global definition doesn't carry workspace-private evidence into global Claim state. Show useful project/Workspace decisions, not only person names.

Deliver complete scripted extraction -> durable candidate -> CLI alternatives/choice -> exact preview/accept -> inspection/replay. Test same-name, alias reuse, unsupported identity, proposed definitions, scope and stale graph revisions, atomic rollback. #142 temporal/corrections and #144 editing/batches come later; don't sweep them in. Save exact original bytes/hashes for touched prior-ticket files and own new-file manifest, freeze for two-axis review/root full verification. No stage/commit/push/PR; no model config or inference.
