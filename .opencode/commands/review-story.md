---
description: Review one story PR through independent evidence-based lenses
agent: review-orchestrator
---

Use the `skill` tool to load `review-story`, then apply it to this candidate:

$ARGUMENTS

This command authorizes read-only local and GitHub inspection plus required
deterministic verification. Never edit or fix files, change Git state, publish a
GitHub comment or review, approve, push, or merge.
